// const app = (commandLineInput) => {
//   //console.log(commandLineInput);
//   if (commandLineInput.add) {
//     if (commandLineInput.movie) {
//       //node app.js --add --movie --actor="roy" --title="jaws"
//       console.log(
//         `we are adding ${commandLineInput.title} with the actor ${commandLineInput.actor}`
//       );
//     } else if (commandLineInput.album) {
//       console.log("we are adding to albums");
//     }
//   } else if (commandLineInput.list) {
//     console.log("we have list");
//   }
// };

// app(argv);

// DB_NAME = mysqlMovies;
// DB_USER = root;
// DB_PASSWORD = Password01;
// DB_HOST = localhost;
// DB_DIALECT = mysql;
